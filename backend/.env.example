# Copy to .env and set your PostgreSQL connection string.
# Example (Neon.tech): postgresql://user:password@host/database?sslmode=require
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
